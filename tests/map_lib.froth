; Test map library

; Create a test map
$ 1 'a : 2 'b : 3 'c : /m

; Restrict to subset of keys
m ['a 'c] restrict! writeln!

; Restrict to single key
m ['b] restrict! writeln!

; Restrict to empty array
m [] restrict! writeln!

; Restrict with non-existent key
m ['a 'z] restrict! writeln!

; Restrict with non-identifier in array (should be ignored)
m ['a 42 "hello" 'b] restrict! writeln!

; Restrict empty map
$ ['a 'b] restrict! writeln!
