; Test that closures (cons pairs) can be compared
; Run with -n to avoid exponential blowup comparing deeply nested stdlib environments

"../lib/io.froth" import

{ 1 } { 1 } = println!
{ 1 } { 2 } = println!
