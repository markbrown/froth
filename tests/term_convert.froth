; Test intern-based conversions between identifiers and binders

; Convert identifier to binder

'foo intern idToBinder writeln!
'+ intern idToBinder writeln!

; Convert binder to identifier
'/bar intern idToIdent writeln!
'/- intern idToIdent writeln!

; Round-trip: identifier -> binder -> identifier
'hello intern idToBinder intern idToIdent writeln!

; Round-trip: binder -> identifier -> binder
'/world intern idToIdent intern idToBinder writeln!
