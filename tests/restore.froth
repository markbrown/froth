; Test restore operator

; Save initial environment
env /saved

; Add bindings
1 /a 2 /b

; Verify bindings exist
a println!
b println!

; Restore original environment
saved restore

; Verify bindings are gone (use in to check)
env 'a in println!
env 'b in println!

; Restore empty environment (lose stdlib too)
; Use print (builtin) before restoring to show env size
$ restore
env # print
